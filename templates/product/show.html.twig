{% extends 'base.html.twig' %}

{% block title %}Product{% endblock %}

{% block content %}
<h1>Product</h1>

<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ product.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ product.name }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ product.description }}</td>
        </tr>
        <tr>
            <th>Price</th>
            <td>{{ product.price }}</td>
        </tr>
        <tr>
            <th>StockQuantity</th>
            <td>{{ product.stockQuantity }}</td>
        </tr>
        <tr>
            <th>ImageUrl</th>

            <td></td>
        </tr>
        <tr>
            <th>CreatedAt</th>
            <td>{{ product.createdAt ? product.createdAt|date('Y-m-d H:i:s') : '' }}</td>
        </tr>
    </tbody>
</table>

<button id="add-to-cart" data-product-id="{{ product.id }}" class="btn btn-success">
    🛒 Ajouter au panier
</button>

<div id="flash-message" style="margin-top: 1em;"></div>

<a href="{{ path('app_product_index') }}">back to list</a>

<a href="{{ path('app_product_edit', {'id': product.id}) }}">edit</a>

{{ include('product/_delete_form.html.twig') }}




<!-- <a href="{{ path('shopping_cart_add', { id: product.id }) }}" class="btn btn-success">
    🛒 Ajouter au panier
</a> -->
<!-- {% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }}">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %} -->
{% endblock %}